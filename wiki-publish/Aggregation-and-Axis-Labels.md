# Aggregation and axis labels (draft)

This page is a **placeholder** for documentation of **intelligent aggregation** (auto-interval from window duration), **YAML overrides** for step and X-axis format, **adaptive axis labels**, **locale cascade**, and **mobile-friendly** horizontal labels.

**Status**: Specification only — behavior is not part of a released card version until implemented and released.

**Source of truth**: [Specification draft in the repository](https://github.com/hello-sebastian/energy-horizon/blob/main/specs/001-aggregation-axis-labels/spec.md) (`specs/001-aggregation-axis-labels/spec.md`).

When the feature ships, this Wiki page will be expanded with end-user option tables and examples aligned with the implementation.
